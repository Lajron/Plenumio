@model TrendingCardVM

<partial name="_TrendingCard" for="@Model" />

@* <div class="card border-0 bg-dark-custom text-light shadow-sm" style="font-size:0.9rem;">
    <!-- Header -->
    <div class="card-header bg-dark-custom border-0 py-2 px-3 text-uppercase fw-bold small text-light">
        Trending Tags
    </div>

    <!-- Body: tags -->
    <div class="card-body bg-dark-custom border-0 d-flex flex-column gap-1 p-1">
        @foreach (var tag in Model.Take(5)) {
            <div class="trending-tag d-flex justify-content-between align-items-center px-2 py-1 bg-dark rounded">
                <a href="@Url.Action("Index", "Posts", new { tag = tag.Name })" class="text-light text-decoration-none fw-medium">
                    @tag.Name
                </a>
                <button class="btn btn-sm btn-outline-light" style="width:60px; padding:0.15rem 0.3rem; font-size:0.8rem;">Follow</button>
            </div>
        }
    </div>

    <!-- Footer -->
    <div class="card-footer bg-dark-custom border-0 py-2 text-center small text-secondary">
        <a href="/posts/recently-viewed" class="text-decoration-none text-light">Check out more trending tags</a>
    </div>
</div> *@
